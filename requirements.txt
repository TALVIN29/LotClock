# Scraper runtime dependency. Fetching is still stdlib urllib (see fetch.py) —
# Scrapling is used only as a parser, for CSS selectors and adaptive element
# relocation. Its browser/stealth fetchers are deliberately NOT installed: TLS
# impersonation and proxy rotation are against this project's scraping stance.
scrapling==0.4.12

# Everything below is for the model/notebook work (train.py, analysis.ipynb)
# and for running the test suite.
jupyter==1.1.1
matplotlib==3.11.0
nbformat==5.10.4
numpy==2.5.1
pandas==3.0.3
pytest==9.1.1
scikit-learn==1.9.0
seaborn==0.13.2
